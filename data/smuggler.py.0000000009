         chunkDir=args['chunk_dir'],
                byteSize=args['bytes']
                )

        f=smuggler_lib.reader()
        
        f.mkcodes(
                args['fname'],
                tmpdir=args['qr_dir'],
                mod=args['mod'],
                scale=args['scale_of_code'],
                chunkDir=args['chunk_dir']
                )
        
        print(args)

    def mkgif(self,args):
        gif=mkgif.mkgif()
        args=self.returnArgsAsDict(args)
        files=[os.path.jo